% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_tree.R
\name{DecisionTreeRegressor}
\alias{DecisionTreeRegressor}
\title{Decision Tree Regressor}
\usage{
DecisionTreeRegressor(x, y, max_depth = 10L, min_leaf_size = 5L)
}
\arguments{
\item{x}{Numeric matrix of predictors (n x d).}

\item{y}{Numeric vector of responses (length n).}

\item{max_depth}{Maximum tree depth (default 10).}

\item{min_leaf_size}{Minimum samples per leaf (default 5).}
}
\value{
An S3 object of class \code{DecisionTreeRegressor}.
}
\description{
Fits a CART regression tree using MSE splitting criterion.
}
\examples{
x <- matrix(rnorm(200), ncol = 2)
y <- x[,1]^2 + rnorm(100, sd = 0.1)
model <- DecisionTreeRegressor(x, y, max_depth = 5)
preds <- predict(model, x)
}
