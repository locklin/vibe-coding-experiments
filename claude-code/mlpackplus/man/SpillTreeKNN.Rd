% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spill_tree_knn.R
\name{SpillTreeKNN}
\alias{SpillTreeKNN}
\title{SpillTree K-Nearest Neighbors}
\usage{
SpillTreeKNN(reference, k, tau = 0, max_leaf_size = 20L)
}
\arguments{
\item{reference}{Numeric matrix of reference points (n x d).}

\item{k}{Number of neighbors to find.}

\item{tau}{Overlap parameter in [0, 0.5). Controls the fraction of
range overlap between children (default 0, exact kd-tree).}

\item{max_leaf_size}{Maximum leaf size (default 20).}
}
\value{
An S3 object of class \code{SpillTreeKNN}.
}
\description{
Builds a spill tree on reference data and finds k-nearest neighbors.
When tau=0, this is equivalent to exact kd-tree search.
}
\examples{
ref <- matrix(rnorm(200), ncol = 2)
model <- SpillTreeKNN(ref, k = 5)
# Query with new points
query <- matrix(rnorm(20), ncol = 2)
result <- predict(model, query)
}
