% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amf.R
\name{AMF}
\alias{AMF}
\title{Alternating Matrix Factorization (NMF via ALS)}
\usage{
AMF(V, rank, max_iter = 100L, tol = 1e-05, lambda = 0.01, seed = NULL)
}
\arguments{
\item{V}{Numeric matrix to factorize (m x n). Should have non-negative entries.}

\item{rank}{Factorization rank (number of latent factors).}

\item{max_iter}{Maximum iterations (default 100).}

\item{tol}{Convergence tolerance on relative Frobenius norm change (default 1e-5).}

\item{lambda}{Ridge regularization parameter (default 0.01).}

\item{seed}{Random seed for reproducibility (default NULL).}
}
\value{
An S3 object of class \code{AMF}.
}
\description{
Performs Non-negative Matrix Factorization using Alternating Least Squares.
Factorizes V ~ W * H where all entries of W and H are non-negative.
}
\examples{
set.seed(42)
W_true <- matrix(runif(30), nrow = 10, ncol = 3)
H_true <- matrix(runif(15), nrow = 3, ncol = 5)
V <- W_true \%*\% H_true
model <- AMF(V, rank = 3, seed = 42)
reconstruction <- predict(model)
}
