% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_forest.R
\name{RandomForestRegressor}
\alias{RandomForestRegressor}
\title{Random Forest Regressor}
\usage{
RandomForestRegressor(
  x,
  y,
  n_trees = 100L,
  max_depth = 10L,
  min_leaf_size = 5L,
  max_features = NULL,
  sample_fraction = 1,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric matrix of predictors (n x d).}

\item{y}{Numeric vector of responses (length n).}

\item{n_trees}{Number of trees (default 100).}

\item{max_depth}{Maximum tree depth (default 10).}

\item{min_leaf_size}{Minimum samples per leaf (default 5).}

\item{max_features}{Number of features to consider at each split
(default floor(d/3)).}

\item{sample_fraction}{Fraction of data to sample per tree (default 1.0).}

\item{seed}{Random seed for reproducibility (default NULL).}
}
\value{
An S3 object of class \code{RandomForestRegressor}.
}
\description{
Fits a random forest for regression using bagged CART trees with
random feature subsets.
}
\examples{
x <- matrix(rnorm(200), ncol = 2)
y <- sin(x[,1]) + 0.5 * cos(x[,2]) + rnorm(100, sd = 0.1)
model <- RandomForestRegressor(x, y, n_trees = 50, seed = 42)
preds <- predict(model, x)
}
