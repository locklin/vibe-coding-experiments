% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlpack_api.R
\name{decision_tree}
\alias{decision_tree}
\title{mlpack-style Decision Tree Interface}
\usage{
decision_tree(
  training_data = NULL,
  training_labels = NULL,
  test_data = NULL,
  input_model = NULL,
  max_depth = 10L,
  min_leaf_size = 5L
)
}
\arguments{
\item{training_data}{Numeric matrix of training predictors (optional).}

\item{training_labels}{Numeric vector of training responses (optional).}

\item{test_data}{Numeric matrix of test predictors (optional).}

\item{input_model}{A previously fitted DecisionTreeRegressor (optional).}

\item{max_depth}{Maximum tree depth (default 10).}

\item{min_leaf_size}{Minimum leaf size (default 5).}
}
\value{
A list with \code{output_model} and optionally \code{predictions}.
}
\description{
Provides a functional interface similar to mlpack's CLI bindings.
Trains and/or predicts depending on which arguments are provided.
}
\examples{
x <- matrix(rnorm(200), ncol = 2)
y <- x[,1]^2 + rnorm(100, sd = 0.1)
# Train and predict in one call
result <- decision_tree(training_data = x, training_labels = y,
  test_data = x, max_depth = 5)
}
