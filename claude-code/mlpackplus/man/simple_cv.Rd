% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{simple_cv}
\alias{simple_cv}
\title{Simple Train/Test Split Cross-Validation}
\usage{
simple_cv(model_fn, x, y, train_fraction = 0.8, metric_fn = mse)
}
\arguments{
\item{model_fn}{A function(x, y) that returns a fitted model.}

\item{x}{Numeric matrix of predictors.}

\item{y}{Numeric vector of responses.}

\item{train_fraction}{Fraction of data for training (default 0.8).}

\item{metric_fn}{A function(actual, predicted) returning a scalar score.}
}
\value{
A list with \code{score}, \code{train_indices}, and
\code{test_indices}.
}
\description{
Evaluates a model using a single train/test split.
}
\examples{
set.seed(42)
x <- matrix(rnorm(200), ncol = 2)
y <- x[,1]^2 + rnorm(100, sd = 0.1)
model_fn <- function(x, y) DecisionTreeRegressor(x, y, max_depth = 5)
result <- simple_cv(model_fn, x, y, train_fraction = 0.8, metric_fn = mse)
}
