% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{kfold_cv}
\alias{kfold_cv}
\title{K-Fold Cross-Validation}
\usage{
kfold_cv(model_fn, x, y, k = 5L, metric_fn = mse)
}
\arguments{
\item{model_fn}{A function(x, y) that returns a fitted model with a
\code{predict} method.}

\item{x}{Numeric matrix of predictors.}

\item{y}{Numeric vector of responses.}

\item{k}{Number of folds (default 5).}

\item{metric_fn}{A function(actual, predicted) returning a scalar score
(lower is better by convention).}
}
\value{
A list with \code{mean_score} and \code{fold_scores}.
}
\description{
Evaluates a model using k-fold cross-validation.
}
\examples{
set.seed(42)
x <- matrix(rnorm(200), ncol = 2)
y <- x[,1]^2 + rnorm(100, sd = 0.1)
model_fn <- function(x, y) DecisionTreeRegressor(x, y, max_depth = 5)
result <- kfold_cv(model_fn, x, y, k = 5, metric_fn = mse)
}
