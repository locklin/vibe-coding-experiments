% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlpack_api.R
\name{random_forest}
\alias{random_forest}
\title{mlpack-style Random Forest Interface}
\usage{
random_forest(
  training_data = NULL,
  training_labels = NULL,
  test_data = NULL,
  input_model = NULL,
  n_trees = 100L,
  max_depth = 10L,
  min_leaf_size = 5L,
  max_features = NULL,
  sample_fraction = 1,
  seed = NULL
)
}
\arguments{
\item{training_data}{Numeric matrix of training predictors (optional).}

\item{training_labels}{Numeric vector of training responses (optional).}

\item{test_data}{Numeric matrix of test predictors (optional).}

\item{input_model}{A previously fitted RandomForestRegressor (optional).}

\item{n_trees}{Number of trees (default 100).}

\item{max_depth}{Maximum tree depth (default 10).}

\item{min_leaf_size}{Minimum leaf size (default 5).}

\item{max_features}{Features per split (default NULL = d/3).}

\item{sample_fraction}{Bootstrap sample fraction (default 1.0).}

\item{seed}{Random seed (default NULL).}
}
\value{
A list with \code{output_model} and optionally \code{predictions}.
}
\description{
Provides a functional interface similar to mlpack's CLI bindings.
}
\examples{
x <- matrix(rnorm(200), ncol = 2)
y <- sin(x[,1]) + rnorm(100, sd = 0.1)
result <- random_forest(training_data = x, training_labels = y,
  test_data = x, n_trees = 50, seed = 42)
}
